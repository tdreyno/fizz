---
"@tdreyno/fizz": minor
---

Add the new `action("Type")` and `action("Type").withPayload<P>()` action-creator API, and deprecate `createAction` while keeping it available for backwards compatibility.