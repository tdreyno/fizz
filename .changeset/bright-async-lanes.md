---
"@tdreyno/fizz": minor
---

Add async scheduled operations with `startAsync` and `cancelAsync`, controlled async drivers for testing, direct success and failure mapping to user actions, `AsyncCancelled` for observable explicit cancellation, and a `requestJSONAsync(...).validate(...).chainToAction(...)` convenience builder for JSON request flows.