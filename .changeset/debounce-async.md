---
"@tdreyno/fizz": minor
---

Add `debounceAsync(...)` for latest-wins debounced async flows with required `asyncId`, automatic in-flight cancellation on replacement, and explicit resolve/reject action mapping.