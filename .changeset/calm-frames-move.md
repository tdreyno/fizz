---
"@tdreyno/fizz": minor
---

<!-- markdownlint-disable-file MD041 MD012 -->

Add requestAnimationFrame loop support with `startFrame()` and `cancelFrame()`, driven by the existing `OnFrame` action and testable through the controlled timer driver.
