---
"@tdreyno/fizz": patch
---

Add chainable DOM mutator return documentation and skills guidance. Updated examples now show fluent effect-return composition like `setValue(...).dispatchEvent(...).mutate(...)` while preserving array-compatible behavior notes.