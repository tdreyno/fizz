---
"@tdreyno/fizz": minor
---

Rename the fluent helper export from `fluentAction(...)` to `action(...)` in `@tdreyno/fizz/fluent`.

This is a breaking API change for fluent users importing `fluentAction`.
